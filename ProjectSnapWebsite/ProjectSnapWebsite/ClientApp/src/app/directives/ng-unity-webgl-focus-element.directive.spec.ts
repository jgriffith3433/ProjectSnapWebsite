import { NgUnityWebglFocusElementDirective } from './ng-unity-webgl-focus-element.directive';

describe('NgUnityWebglFocusElementDirective', () => {
  it('should create an instance', () => {
    const directive = new NgUnityWebglFocusElementDirective();
    expect(directive).toBeTruthy();
  });
});
